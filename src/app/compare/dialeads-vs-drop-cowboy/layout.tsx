import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dialeads vs Drop Cowboy – Complete Comparison 2025",
  description:
    "Comparing Dialeads vs Drop Cowboy for ringless voicemail drops. See which platform offers better AI calling, CRM sync, SMS follow-up, compliance tools, and pricing for your team.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
