import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dialeads vs CallTools – Complete Comparison 2025",
  description:
    "CallTools is an established predictive dialer built for traditional call centers. Dialeads is built for modern outbound teams that want AI voice agents, live objection rebuttals, and automated multi-channel follow-up. Here is the complete honest comparison.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
