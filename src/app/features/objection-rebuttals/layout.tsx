import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Live Objection Rebuttals & AI Sales Copilot – Dialeads",
  description:
    "Dialeads surfaces real-time AI rebuttal scripts the moment a prospect pushes back. Sub-200ms transcription, battle-tested playbooks, and 1-click copy for any objection.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
